{
  "name": "my-feishu-blog",
  "version": "1.0.0",
  "scripts": {
    "dev": "feishu-pages && vitepress dev docs",
    "build": "export FEISHU_API_HOST=https://open.larksuite.com && feishu-pages && vitepress build docs"
  },
  "dependencies": {
    "feishu-pages": "latest",
    "vitepress": "^1.0.0"
  }
}
